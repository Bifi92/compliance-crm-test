<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Empresa</th>
      <th>Telefone</th>
      <th>Celular</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @leads.each do |lead| %>
      <tr>
        <td><%= lead.to_s %></td>
        <td><%= lead.empresa %></td>
        <td><%= lead.telefone %></td>
        <td><%= lead.celular %></td>
        <td><%= link_to 'Show', lead, :class => 'btn btn-default btn-sm' %></td>
        <td><%= link_to 'Edit', edit_lead_path(lead), :class => 'btn btn-default btn-sm' %></td>
        <td><%= link_to 'Destroy', lead, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-default btn-sm'  %></td>
      </tr>
    <% end %>
  </tbody>
</table>